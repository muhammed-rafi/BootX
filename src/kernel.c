#include "kernel.h"

void kernel_main() {
    // const char *str = "Hello, World!";
    // char *vidptr = (char *)0xb8000;  // Video memory begins here.
    // unsigned int i = 0;
    // unsigned int j = 0;

    // // Clear the screen by writing spaces to video memory.
    // while (j < 80 * 25 * 2) {
    //     vidptr[j] = ' ';     // Character byte
    //     vidptr[j + 1] = 0x07; // Attribute byte: light grey on black background
    //     j += 2;
    // }

    // j = 0;

    // // Write the string to video memory.
    // while (str[i] != '\0') {
    //     vidptr[j] = str[i];     // Character byte
    //     vidptr[j + 1] = 0x07;   // Attribute byte: light grey on black background
    //     ++i;
    //     j += 2;
    // }

    // return;
}